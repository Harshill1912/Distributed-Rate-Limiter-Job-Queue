[pytest]
# Put the project root on sys.path so `import app` / `import worker` work when
# pytest is invoked as the `pytest` console script (as CI does), not just via
# `python -m pytest`.
pythonpath = .
testpaths = tests
